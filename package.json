{
  "name": "bu-selected-flask",
  "version": "1.0.0",
  "scripts": {
    "dev": "pip install -r requirements.txt && python3 app.py",
    "start": "python3 app.py",
    "build": "echo 'No build step required for Flask'"
  }
}
